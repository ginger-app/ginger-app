export type SupplierPreviewItem = {
    name: string;
    unit: string;
    price: string;
    stock: string;
    categoryName: string;
};

export type SuplierItem = {
    name: string;
    id: string;
    unit: string;
    price: number;
    stock: number;
    category: string;
    image: string;
};
